# Copy to .env and fill in. .env is git-ignored — never commit real credentials.
#
# Anything already exported in your shell overrides this file, which is how a
# benchmark run can target one model for one invocation:
#   AISC_LLM_MODEL=gpt-4.1-mini python -m extract_eval.regression.evaluate_regression

# ── LLM (required by the extraction pipeline, API server and benchmark) ───
# Works with Azure OpenAI, api.openai.com, and any OpenAI-compatible server
# (vLLM, Ollama, LM Studio). Leave the endpoint empty to use api.openai.com.
# Extract + validate use AISC_LLM_*. The regression FP judge uses AISC_JUDGE_*
# when set (falls back to AISC_LLM_* if judge fields are empty).
AISC_LLM_ENDPOINT=
AISC_LLM_API_KEY=
AISC_LLM_MODEL=gpt-4o-mini

# Max concurrent per-chunk LLM calls during extract (default 4). Lower this
# on free-tier OpenRouter / tight RPM limits. CLI: --max-parallel-chunks.
AISC_MAX_PARALLEL_CHUNKS=4

# Stronger model for extract_eval.regression.judge (advisory FP auditor only).
# Leave blank to reuse AISC_LLM_*. Endpoint/key blank → reuse AISC_LLM_ENDPOINT/KEY.
AISC_JUDGE_ENDPOINT=
AISC_JUDGE_API_KEY=
AISC_JUDGE_MODEL=gpt-4o

# ── MISP (only needed for data collection: explore_misp.py) ───────────────
MISP_URL=
MISP_API_KEY=
MISP_VERIFY_SSL=false

# ── Advisory CWE lookup (optional; raises NVD / GitHub rate limits) ───────
# Used by extract_pipeline.advisory_cwe during validate (CVE→NVD, GHSA→GitHub).
NVD_API_KEY=
GITHUB_TOKEN=

# ── Optional: push benchmark results to a Google Sheet ────────────────────
AISC_SHEET_ID=
AISC_SHEET_GID=
